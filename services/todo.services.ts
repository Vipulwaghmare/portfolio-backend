import Todo from "../models/Todo.model";

const todoServices = {
  getUserTodos: async (userId) => {},
  addUserTodos: async (userId) => {},
  deleteUserTodos: async (userId) => {},
};

export default todoServices;
